# 项目运行说明

本项目是陈颖教授个人主页，基于 React 19 + TypeScript + Vite 构建的纯前端静态网站。

---

## 环境要求

- **Node.js** 18 或更高版本
- **npm** 8 或更高版本（随 Node.js 自动安装）

> 验证版本：
> ```bash
> node -v
> npm -v
> ```

---

## 首次使用：安装依赖

```bash
cd Chen-main
npm install
```

> 若 `node_modules` 目录已存在，可跳过此步骤。

---

## 启动开发服务器（日常开发）

```bash
npm run dev
```

启动后终端会显示：

```
  VITE v6.x.x  ready in xxx ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
```

打开浏览器访问 **http://localhost:5173** 即可查看网站。

- 修改 `src/` 下的文件后，页面会**自动热更新**，无需刷新。
- 按 `Ctrl + C` 停止服务器。

---

## 构建生产版本

```bash
npm run build
```

构建产物输出到 `dist/` 目录，可部署到任意静态托管服务（如 GitHub Pages）。

---

## 预览生产构建结果

```bash
npm run preview
```

在本地启动静态服务器预览 `dist/` 目录的内容，访问地址通常为 **http://localhost:4173**。

---

## 代码检查

```bash
npm run lint
```

使用 ESLint 检查代码规范问题。

---

## 页面路由说明

| 路径 | 页面 |
|------|------|
| `/` | 主页（个人简介） |
| `/research` | 研究方向 |
| `/teaching` | 教学课程 |
| `/events` | 活动与新闻 |
| `/member` | 团队成员 |
| `/research/fuelture` | Fuelture 项目详情 |

---

## 项目结构简介

```
Chen-main/
├── src/
│   ├── pages/       # 各页面组件
│   ├── components/  # 通用组件
│   ├── data/        # 静态数据（团队成员、活动等）
│   └── App.tsx      # 路由配置入口
├── public/          # 静态资源（图片、PDF等）
├── dist/            # 构建输出（运行 build 后生成）
└── package.json     # 依赖与脚本配置
```
